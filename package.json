{
  "name": "correspondencia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "cd backend; npm run start & cd ../frontend; npm run lint; npm run serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
  }
 }
